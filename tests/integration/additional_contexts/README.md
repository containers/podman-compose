# Test podman-compose with build.additional_contexts

```
podman-compose build
podman-compose up
podman-compose down
```

The expected output is

```
[dict] | Data for dict
[list] | Data for list
```
