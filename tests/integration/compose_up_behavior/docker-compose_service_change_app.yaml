services:
  app:
    image: nopush/podman-compose-test
    command: ["dumb-init", "/bin/busybox", "httpd", "-f", "-p", "8080"]
    environment:
      - SERVICE_CHANGE=true
    depends_on:
      - db
  db:
    image: nopush/podman-compose-test
    command: ["dumb-init", "/bin/busybox", "httpd", "-f", "-p", "8080"]
  no_deps:
    image: nopush/podman-compose-test
    command: ["dumb-init", "/bin/busybox", "httpd", "-f", "-p", "8080"]
